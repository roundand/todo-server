var config = {}

config.mongo = {};

// the following connection string should be replaced with something valid, eg from mongolabs.com
// see http://docs.mongodb.org/manual/reference/connection-string/ for reference
config.mongo.connectionString = 'mongodb://todomongodb;
module.exports = config;
