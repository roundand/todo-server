var config = {}

config.mongo = {};

// the following connection string should be replaced with something valid, eg from mongolabs.com
config.mongo.connectionString = 'mongodb://{username}:{password}@{domain}:{port}/{db}';
module.exports = config;
